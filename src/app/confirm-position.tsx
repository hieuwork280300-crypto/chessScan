import { StubScreen } from '@/components/ui/StubScreen';

export default function ConfirmPosition() {
  return <StubScreen title="Confirm position" note="Editable board + FEN — next build step." />;
}
